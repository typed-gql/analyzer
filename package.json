{
  "private": true,
  "name": "@typed-gql/analyzer",
  "version": "0.1.0",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json && cpy README.md dist && package-json-minifier",
    "test": "tsx test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/typed-gql/analyzer.git"
  },
  "license": "MIT",
  "peerDependencies": {
    "@typed-gql/parser": "^0.1.0"
  },
  "peerDependenciesMeta": {
    "@typed-gql/parser": {
      "optional": true
    }
  },
  "devDependencies": {
    "@typed-gql/lexer": "^0.1.3",
    "@types/node": "^22.10.2",
    "cpy-cli": "^5.0.0",
    "package.json-minifier": "^0.0.4",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
